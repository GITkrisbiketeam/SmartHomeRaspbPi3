<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="viewModel"
            type="com.krisbiketeam.smarthomeraspbpi3.viewmodels.AddStorageHomeUnitViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">
        <!--android:visibility="@{viewmodel.dataLoading ? View.GONE : View.VISIBLE}"-->

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/add_storage_text_name"
            android:textColor="@color/colorPrimaryDark" />

        <EditText
            android:id="@+id/storage_unit_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/add_storage_text_title_hint"
            android:inputType="text"
            android:maxLines="1"
            android:text="@={viewModel.name}"
            android:textAppearance="@style/TextAppearance.AppCompat.Title" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/add_storage_text_description"
            android:textColor="@color/colorPrimaryDark" />

        <EditText
            android:id="@+id/add_task_description"
            android:layout_width="match_parent"
            android:layout_height="350dp"
            android:gravity="top"
            android:hint="@string/add_storage_text_description_hint"
            android:inputType="text"
            android:text="@={viewModel.firebaseTableName}" />

    </LinearLayout>

</layout>